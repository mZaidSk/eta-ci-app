name: c_i
description: Flutter app scaffold with Provider, Dio, FVM, and json_serializable
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  dio: ^5.6.0
  json_annotation: ^4.9.0

# Optional for dotenv-like config; comment out if unused
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  json_serializable: ^6.8.0

